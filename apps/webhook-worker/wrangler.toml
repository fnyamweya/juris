name = "juris-webhooks"
main = "src/index.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

[observability]
enabled = true

[vars]
ENVIRONMENT = "development"
LOG_LEVEL = "debug"

# ── UAT ──
[env.uat]
name = "juris-webhooks-uat"
[env.uat.vars]
ENVIRONMENT = "uat"
LOG_LEVEL = "debug"

# ── Staging ──
[env.staging]
name = "juris-webhooks-staging"
[env.staging.vars]
ENVIRONMENT = "staging"
LOG_LEVEL = "info"

# ── Production ──
[env.production]
name = "juris-webhooks"
[env.production.vars]
ENVIRONMENT = "production"
LOG_LEVEL = "warn"
