# Changesets

Juris uses [Changesets](https://github.com/changesets/changesets) for version management and changelog generation.

Run `pnpm changeset` to create a new changeset when making a notable change.
